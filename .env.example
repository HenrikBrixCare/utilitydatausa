# Existing production OpenAI integration; do not commit real keys.
OPENAI_API_KEY=
# Optional EIA state-average electricity prices. Free registration requires owner email.
EIA_API_KEY=
# Optional higher USGS modern-API quota; legacy USGS and other current sources need no key.
USGS_API_KEY=
# Production uses Vercel OIDC to access the deployed Supabase storage bridge.
# Optional emergency switch; leave unset/0 to enable in production.
SUPABASE_PROFILE_STORAGE_DISABLED=0
